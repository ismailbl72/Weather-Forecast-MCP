# 🏋️‍♂️ FitPersona - Kişiye Özel Spor ve BMI Asistanı
startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for FitPersona MCP
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({ command: 'python', args: ['-m', 'server'] })
  exampleConfig: {}